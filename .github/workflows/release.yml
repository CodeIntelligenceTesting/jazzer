name: Release

on:
  workflow_dispatch:
  release:
    types: [released]

jobs:
  docker_push:
    runs-on: ubuntu-latest
    needs: deploy

    steps:
      - uses: actions/checkout@v3

      - name: Docker login
        run: echo "${{ secrets.DOCKER_TOKEN }}" | docker login --user ${{ secrets.DOCKER_USER }} --password-stdin

      - name: Push docker containers
        run: docker/push_all.sh
